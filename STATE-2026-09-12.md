# State as of 2026-09-12

One page, so any session or co-author can pick this up without reading a transcript.

## The manuscript

`paper/latex/main.tex` compiles to exactly 10 pages, 0 errors, 0 undefined references,
0 em dashes. `python experiments/numbers_audit.py` exits 0 with no FAIL verdicts.
`python paper/make_arxiv_source.py` builds a 10-page source tree with no residual review
markup. Standing inventory holds: ten limitations, eight findings rows, six disposal rows,
six taxonomy classes, 51 citations.

`python reproduce.py` was run end to end on 2026-09-12 with no network and no API access: contract validation passes on 48 files (144 checks skipped, the expected offline subset), both generated-table verifiers re-derive and match, the numbers audit reports no FAIL, and 424 tests pass. It ends with `reproduce-results: OK`. On Windows without make this is the path a stranger takes; the Makefile sets `PYTHONPATH` itself, so invoking the analysis scripts directly without it will fail on an import and is not a defect.

Zichong Wang's five comments are in the source verbatim at lines 167, 178, 179, 208 and 216,
each with a reply beside it. They are invisible in the submission PDF and stripped from the
arXiv source by `make_arxiv_source.py`.

## What was done on 2026-09-12

Four checklist passes from `paper-review-lessons`, then a five-seat review panel, then six
rounds of repairs. Each finding was verified in the file or against upstream source at a
pinned commit before it was acted on; several did not survive that check and were dropped
rather than fixed.

The repairs that changed a claim rather than a sentence:

- Table V counted two AgentDojo rows that `FINDINGS-VERIFIED.md:367` says must not enter any
  score-impact claim. No shipped travel task calls `reserve_car_rental`. Both rows are now
  disclosed as field-name matches bounding what the trace rule selects, not what the defect
  reaches, and the paper says the rule is pre-registered rather than re-specified after
  seeing which rows it returns.
- The conclusion claimed defects across six executable classes where four are field-observed.
- A census claim about tool coverage was false: 19 tau2 tools are airline, retail and telecom
  only, excluding banking_knowledge's 42 WRITE tools, mock's 2 and telecom user tools' 15.
- The abstract's precision figure is a pooled toy-plus-real sample, which the method section
  forbids from entering a headline. Now disclosed there.
- The agreement paragraph reported 98 of 98 beside a flip in the same stratum. The flip is
  outside that denominator and the filter excluding it landed with the result, so the
  unfiltered count, 98 of 99, now sits beside it.
- Table I credited 27 categories to three works that account for 17.
- The checker's UNTESTABLE share is now reported: 96 of 180 clauses on tau2-bench, 11 of 60
  on AgentDojo, 4 of 27 on MM-ToolSandbox.
- AgentProp-Bench (arXiv 2604.16706) was logged in `REFERENCES.md` and never cited, though
  its title collides with this paper's claim. Verified against the source and disposed.

## Drift between the two manuscripts

`paper/latex/main.tex` and `paper/main.md` have been edited independently for weeks and today
produced three drifts that `numbers_audit.py` does not catch, because it cross-checks nine named
quantities rather than the prose. All three are now fixed, and the lesson is worth keeping: a
correction has to be propagated across `paper/latex/main.tex`, `paper/main.md`,
`SUBMISSION-PACKAGE.md` and `report/disclosure_log.md`, not just the first of them.

- The .md carried both AgentDojo score-at-risk rows with no disclosure after the .tex got one.
- The .md said 0/27 category overlap where the three works account for 17.
- The .md disclosed that the comparability filter is unreconciled with the protocol rule counting
  UNTESTABLE against anything as a disagreement, and the .tex did not. The .tex now does; this is
  the paragraph most exposed to a reviewer who knows the author has been rejected for a post-hoc
  rule beside an agreement statistic.

To look for it again, compare every distinctive number in one file against the other and read
whatever appears in only one. Most hits are TikZ coordinates in the .tex and DOIs in the .md; the
few that are neither are worth a human read.

## What needs the author

Checked in the files on 2026-09-12 rather than carried forward from an older task list. Two items
that had been tracked as blockers were already done:

- **Zichong Wang's affiliation is filled.** `paper/latex/main.tex:138-146` has no empty
  affiliation block and the compiled page one renders all three authors at Florida International
  University, Miami. Review is single-blind, so that block ships as written.
- **The back-matter paragraphs are written**, both the AI-use statement and the coordinated
  disclosure paragraph. Neither is a placeholder, and the call for papers requires neither.
- **Venue confirmations are recorded** for every cited entry. What remains is a recheck of five
  preprints in case any has been accepted since the last pass, not eleven open confirmations.

Two items are genuinely open, in the order I would take them:

1. **File the four disclosure issues.** Text is ready in `SUBMISSION-PACKAGE.md`.
   `report/disclosure_log.md` records that nothing is filed. The paper names four projects as
   shipping defective code, one of them a clinical benchmark published in NEJM AI. Under the
   paper's own principle that either side of a divergence may be repaired, a maintainer's
   answer is part of the finding rather than commentary on it.
2. **Decide on `repro/env/refsol.py`.** It is git-tracked in the public MIT repository and in
   the Zenodo deposit. Upstream `README.md:45` routes users to a Stanford Medicine Box link
   instead, so gating it is plausibly a contamination control, and the paper says elsewhere
   that audited benchmarks are not redistributed. Options: remove it and ship the SHA-256 plus
   the retrieval instruction; keep it with written permission; or keep it and argue the case
   in print. Zenodo versions are immutable, so removal means a new version.
3. **Scopus recheck**, optional, on five cited preprints in case any has been accepted since
   the last pass: `evidencebounds26`, `judgesense26`, `rised26`, `tau2bench25`, `abc26`.

Then submit, and post the preprint the same week rather than after notification.

## Two citations deliberately left out

RICK (`tiwari24rick`, IEEE TSE 2024) and the mock-assertions paper (`zhu25mockassertions`,
ACM PACMSE 2025) are both closer to this paper's framing than several works Table I does
dispose of. Both publisher pages return HTTP 403 and `REFERENCES.md` records no complete
author list for either. Adding them would mean inventing authors, which is worse than the
gap. If Scopus access returns, they are worth adding.
