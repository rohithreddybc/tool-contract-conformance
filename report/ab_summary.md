# Agent-impact experiment (Tier 1) -- results

Generated by `experiments/ab_run.py` at commit `083802650766f6b474544fbcf86bac9267656f0d`, 2026-08-29T01:10:15Z.

Bound by `experiments/analysis_plan.md` (pre-registered, untouched by this run). Tier 2 not built, per build instructions.

## 1. Static task selection (exhaustive, pre-registered rule)

| Finding | Domain:Tool | Tasks total | Affected (experiment frame) | score_at_risk cross-check |
|---|---|---:|---:|---|
| F2 | telecom:refuel_data | 2285 | 1120 | agrees |
| F3 | airline:cancel_reservation | 50 | 7 | agrees |

**F2** (FINDINGS-VERIFIED.md Finding 2): selection rule = gold reference solution (evaluation_criteria.actions) invokes this tool -- exhaustive over the full on-disk task file, no exclusions.

**F3** (FINDINGS-VERIFIED.md Finding 3): selection rule = gold reference solution (evaluation_criteria.actions) invokes this tool -- exhaustive over the full on-disk task file, no exclusions.

## 2. Patch verification (mechanical, no model)

### F2 -- telecom:refuel_data

- probe args: `{'customer_id': 'C1001', 'line_id': 'L1003', 'gb_amount': 5.0}`
- unpatched: `{'success': True, 'error': None, 'data_refueling_gb_changed': True}`
- patched: `{'success': False, 'error': 'ValueError: Line must be active to refuel data', 'data_refueling_gb_changed': False}`
- defect reproduces unpatched: **True**
- patch fixes defect: **True**
- PASS: **True**

### F3 -- airline:cancel_reservation

- probe args: `{'reservation_id': '4WQ150'}`
- unpatched: `{'success': True, 'seats_after': 0}`
- patched: `{'success': True, 'seats_after': 3}`
- defect reproduces unpatched: **True**
- patch fixes defect: **True**
- PASS: **True**

## 3. Deviation from pre-registration

Document: `experiments/analysis_plan.md sec 4`

experiments/analysis_plan.md sec 4 specifies ONE recorded trajectory per task, sourced from tau2's own orchestrator (a real LLMAgent + UserSimulator call through litellm). This project runs with no model and no API credentials anywhere (CLAUDE.md); a prior run of this script (commit 94adf1e) confirmed that path cannot complete here -- record_trajectory failed on every attempted task with 'OpenAIException - Missing credentials'. DEVIATION: this run instead sources the action sequence from each affected task's OWN reference solution (task.evaluation_criteria.actions -- tau2's own gold trajectory, the same one EnvironmentEvaluator.calculate_reward itself replays to build the target/gold environment), executes it once against a fresh, unpatched, task-scoped environment via Tau2Adapter.record_reference_trajectory, and replays the identical recorded calls against as-shipped and patched tools with tau2's own EnvironmentEvaluator, unmodified. This is a STRENGTHENING of the plan's own determinism claim, not a weaker substitute: plan sec 5 says 'The model selected which trajectory exists; it plays no part in the comparison' -- here no model selects the trajectory either, so the model is removed from the evidence path entirely, including at recording time. It remains a deviation from a committed, pre-registered document and is recorded as such here, not silently substituted.

## 4. Trajectory recording (model-free -- reference-solution action sequences)

| Finding | Mode | Population | Selected (N) | Seed |
|---|---|---:|---:|---|
| F2 | seeded_sample | 1120 | 5 | 1 |
| F3 | seeded_sample | 7 | 5 | 1 |

- trajectories recorded: **10**
- discarded for infrastructure failure: **0** (logged individually below; a discard is never a silent exclusion -- the task remains in static_task_selection)

## 5. Outcome-cell table (overall, across evaluator bases)

| Cell | Count |
|---|---|
| not_at_risk | n/a -- population is pre-filtered to the experiment frame (gold invokes the defective tool); see analysis/score_at_risk.py for the separate at-risk population |
| at_risk_not_exercised | 10 |
| exercised_no_flip | 0 |
| flipped | 0 |

## 6. Outcome-cell table PER EVALUATOR BASIS

`gold_vs_agent_db_comparison` (airline: DB reward compares gold-execution end state against agent-execution end state; both sides here run the SAME tool implementation, so no flip is possible by construction -- a null here is evidence about the oracle's structure, not evidence the defect is harmless). `per_task_state_assertion` (telecom: ENV_ASSERTION reward runs a named assertion against the agent-execution end state alone, no gold comparison -- a flip here IS possible.)

| Finding\|Basis | not_at_risk | at_risk_not_exercised | exercised_no_flip | flipped |
|---|---|---:|---:|---:|
| F2|per_task_state_assertion | n/a -- population pre-filtered to the experiment frame, a confirmed subset of at-risk | 5 | 0 | 0 |
| F3|gold_vs_agent_db_comparison | n/a -- population pre-filtered to the experiment frame, a confirmed subset of at-risk | 5 | 0 | 0 |

## 7. Defect-exercise rate

| Finding | Trajectories scored | Exercised | Exercise rate |
|---|---:|---:|---:|
| F2 | 5 | 0 | 0.0% |
| F3 | 5 | 0 | 0.0% |

## 8. Flips

Across 10 recorded trajectories on 10 affected tasks, no verdict changed under the patched tools; the defects documented in FINDINGS-VERIFIED.md are latent under the reference-solution action sequences these benchmarks' own gold trajectories take, and the exercise rates in sec 7 show why (experiments/analysis_plan.md sec 3's committed null-result framing).
