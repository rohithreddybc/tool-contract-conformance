# Agent-impact experiment (Tier 1) -- results

Generated by `experiments/ab_run.py` at commit `94adf1e4d0189b4c3840e6ccef409f469850503e`, 2026-08-28T23:34:13Z.

Bound by `experiments/analysis_plan.md` (pre-registered, untouched by this run). Tier 2 not built, per build instructions.

## 1. Static task selection (exhaustive, pre-registered rule)

| Finding | Domain:Tool | Tasks total | Affected (experiment frame) | score_at_risk cross-check |
|---|---|---:|---:|---|
| F2 | telecom:refuel_data | 2285 | 1120 | agrees |
| F3 | airline:cancel_reservation | 50 | 7 | agrees |

**F2** (FINDINGS-VERIFIED.md Finding 2): selection rule = gold reference solution (evaluation_criteria.actions) invokes this tool -- exhaustive over the full on-disk task file, no exclusions.

**F3** (FINDINGS-VERIFIED.md Finding 3): selection rule = gold reference solution (evaluation_criteria.actions) invokes this tool -- exhaustive over the full on-disk task file, no exclusions.

## 2. Patch verification (mechanical, no model)

### F2 -- telecom:refuel_data

- probe args: `{'customer_id': 'C1001', 'line_id': 'L1003', 'gb_amount': 5.0}`
- unpatched: `{'success': True, 'error': None, 'data_refueling_gb_changed': True}`
- patched: `{'success': False, 'error': 'ValueError: Line must be active to refuel data', 'data_refueling_gb_changed': False}`
- defect reproduces unpatched: **True**
- patch fixes defect: **True**
- PASS: **True**

### F3 -- airline:cancel_reservation

- probe args: `{'reservation_id': '4WQ150'}`
- unpatched: `{'success': True, 'seats_after': 0}`
- patched: `{'success': True, 'seats_after': 3}`
- defect reproduces unpatched: **True**
- patch fixes defect: **True**
- PASS: **True**

## 3. Trajectory recording

- `telecom:[mobile_data_issue]data_usage_exceeded[PERSONA:Easy]` -> **failed**
  - error: `tau2 worker reported an error for cmd 'record_trajectory': InternalServerError: litellm.InternalServerError: InternalServerError: OpenAIException - Missing credentials. Please pass an `api_key`, `workload_identity`, `admin_api_key`, or set the `OPENAI_API_KEY` or `OPENAI_ADMIN_KEY` environment variable.`
- `airline:7` -> **failed**
  - error: `tau2 worker reported an error for cmd 'record_trajectory': InternalServerError: litellm.InternalServerError: InternalServerError: OpenAIException - Missing credentials. Please pass an `api_key`, `workload_identity`, `admin_api_key`, or set the `OPENAI_API_KEY` or `OPENAI_ADMIN_KEY` environment variable.`
- F2: 1119 further affected task(s) NOT attempted -- the representative attempt on the first affected task failed with the error recorded in trajectory_recording.attempted above; the failure is an environment/credential blocker (see stopped_reason), not something specific to that task, so repeating the identical failing call for every remaining affected task would burn time for no new information. Not a decision to exclude these tasks from the experiment frame -- they remain listed in static_task_selection and are recordable as soon as the blocker is resolved.
- F3: 6 further affected task(s) NOT attempted -- the representative attempt on the first affected task failed with the error recorded in trajectory_recording.attempted above; the failure is an environment/credential blocker (see stopped_reason), not something specific to that task, so repeating the identical failing call for every remaining affected task would burn time for no new information. Not a decision to exclude these tasks from the experiment frame -- they remain listed in static_task_selection and are recordable as soon as the blocker is resolved.

## 4. Outcome-cell table

| Cell | Count |
|---|---|
| not_at_risk | not computable -- 0 trajectories recorded |
| at_risk_not_exercised | not computable -- 0 trajectories recorded |
| exercised_no_flip | not computable -- 0 trajectories recorded |
| flipped | not computable -- 0 trajectories recorded |

## Stopped at: `trajectory_recording`

tau2 worker reported an error for cmd 'record_trajectory': InternalServerError: litellm.InternalServerError: InternalServerError: OpenAIException - Missing credentials. Please pass an `api_key`, `workload_identity`, `admin_api_key`, or set the `OPENAI_API_KEY` or `OPENAI_ADMIN_KEY` environment variable.

**This is not the pre-registered null result.** experiments/analysis_plan.md sec 3's null sentence presupposes trajectories were recorded and none flipped. Here, zero trajectories were recorded at all -- the blocker is environmental (no live-model credentials in this run environment), not a finding about agent behaviour. The static population, the patches, and the exercise/replay/scoring machinery are all built and verified against synthetic and mechanical probes; re-running `python -m experiments.ab_run` with `OPENAI_API_KEY` (or `AB_LLM_AGENT`/`AB_LLM_USER` pointed at a reachable model) set will attempt the real recordings and populate this table for real.

## Plan items not implementable as written

- **F2 (telecom:refuel_data) recording sample size**: the static, exhaustive selection rule (experiments/analysis_plan.md sec 6) returns 1120 affected tasks for this finding; the plan commits to recording exactly one trajectory per affected task with no re-rolls and no post-hoc exclusion, but does not state a sample size for a population this large, nor a pre-registered subsampling rule. Recording all 1120 would need that rule written and committed BEFORE recording starts (the same reasoning that forbids selecting on observed behaviour elsewhere in the plan applies to picking a subset after seeing the population size). Not resolved by this script; flagged for the co-authors rather than decided unilaterally.
