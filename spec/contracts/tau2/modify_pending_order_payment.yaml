# modify_pending_order_payment, tau2-bench retail, commit c3398666e6559e3a063da3fc04b5acf7f941464e.
# Line numbers verified via `git show c3398666:src/tau2/domains/retail/tools.py`.

contract_version: "1.0"
tool: modify_pending_order_payment
benchmark: tau2-bench
commit: c3398666
source:
  file: src/tau2/domains/retail/tools.py
  start_line: 544
  end_line: 622
  note: "start_line is the @is_tool(ToolType.WRITE) decorator -- itself part of the declared contract."

signature:
  args:
    order_id: { type: str, effective: true }
    payment_method_id: { type: str, effective: true }

preconditions:
  - id: pre.order_pending
    text: "the order must be pending"
    predicate: "pre.orders[args.order_id].status == 'pending'"
    provenance:
      surface: tool_return
      file: src/tau2/domains/retail/tools.py
      line: 569
      quote: "Non-pending order cannot be modified"

effects:
  - id: eff.payment_method_changed
    text: "the order's most recent payment record uses the newly requested payment method"
    predicate: "post.orders[args.order_id].payment_history[-1].payment_method_id == args.payment_method_id"
    provenance:
      surface: docstring
      file: src/tau2/domains/retail/tools.py
      line: 550
      quote: "Modify the payment method of a pending order."

frame:
  - id: frame.items_unchanged
    path: state.orders.*.items
    text: "modifying the payment method never touches order items"
    mode: unchanged
    inferred: true

on_precondition_violation:
  expect:
    error_signal: true
    state_delta: none
    error_type: ValueError
  provenance:
    surface: tool_return
    file: src/tau2/domains/retail/tools.py
    line: 569
    quote: "Non-pending order cannot be modified"

success_signal:
  predicate: "'error' not in result"
  biconditional: true
  justification: >
    Every raise (non-pending order, unknown payment method, payment history not exactly one
    payment, new method identical to the old one, insufficient gift-card balance -- lines
    569-591) occurs before the mutation block at lines 594-620; no raise is reachable after it.
    No effect on this tool is advertised as deferred.
  provenance:
    surface: docstring
    file: src/tau2/domains/retail/tools.py
    line: 550
    quote: "Modify the payment method of a pending order."

reset:
  predicate: "post == pre"
  scope: per_task

notes: >
  Candidate-finding scan: no docstring-vs-body divergence found. retail/policy.md's "Modify
  payment" section ("The original payment method will be refunded immediately if it is a gift
  card, otherwise it will be refunded within 5 to 7 business days") matches the body: both the
  new payment method's gift-card deduction (lines 610-612) and the old payment method's
  gift-card credit (lines 618-620) are synchronous when GiftCard, and silently deferred to a
  real-world cycle otherwise -- the same advertised-deferral pattern as cancel_pending_order.
