# remove_reminder, MM-ToolSandbox tool_sandbox tier, commit 1e8e9324abcb741cc6a9718f9e7c1b80e85a1363.
# NOT a finding-bearing tool -- one of the "roughly four more mutating tools per benchmark that
# are not known to be broken" CLAUDE.md's build spec calls for. scenario_id "tool_sandbox".

contract_version: "1.0"
tool: remove_reminder
benchmark: mm-toolsandbox
commit: 1e8e9324abcb741cc6a9718f9e7c1b80e85a1363
source:
  file: mmtoolsandbox/tools/tool_sandbox/reminder.py
  start_line: 304
  end_line: 328
  note: "start_line is the @register_as_tool(...) decorator -- itself part of the declared contract."

signature:
  args:
    reminder_id:
      type: str
      effective: false
      provenance:
        surface: docstring
        file: mmtoolsandbox/tools/tool_sandbox/reminder.py
        line: 318
        quote: "String format unique identifier of the reminder to be removed"

preconditions:
  - id: pre.reminder_exists
    text: "the reminder id must already exist"
    predicate: "any(r.reminder_id == args.reminder_id for r in pre.REMINDER)"
    provenance:
      surface: docstring
      file: mmtoolsandbox/tools/tool_sandbox/reminder.py
      line: 318
      quote: "String format unique identifier of the reminder to be removed"

effects:
  - id: eff.reminder_removed
    text: "the reminder with the caller-supplied id is removed from the reminder table"
    predicate: "not any(r.reminder_id == args.reminder_id for r in post.REMINDER)"
    provenance:
      surface: docstring
      file: mmtoolsandbox/tools/tool_sandbox/reminder.py
      line: 315
      quote: "Remove a reminder given its unique identifier"

on_precondition_violation:
  expect:
    error_signal: true
    state_delta: none
    error_type: NoDataError
  provenance:
    surface: docstring
    file: mmtoolsandbox/tools/tool_sandbox/reminder.py
    line: 321
    quote: "If the provided reminder_id was not found"

success_signal:
  predicate: "result == None or 'error' not in result"
  biconditional: false

notes: >
  pre.reminder_exists is genuinely enforced (ExecutionContext.remove_from_database raises
  NoDataError before any row is removed when the predicate matches nothing, common/
  execution_context.py:1143-1163) -- expected CONFORMS on the check_precondition_enforcement
  path. Not known to be broken; included to keep the MM-ToolSandbox mutation corpus non-circular.
