Scores at Risk: When Benchmark Tools Violate

         Their Own Advertised Semantics

              Executable Tool-Contract Conformance Testing for Agentic Benchmarks

Rohith Reddy                                                            Wenbin Zhang

   Abstract--Agentic benchmarks score an agent by executing             (src/server/tasks/medagentbench/__init__.py,
tool calls against a simulated environment and reading the              lines 85­91 at commit 9926011, abridged):
resulting state, assuming the tool did what its interface advertised.
None of the audits we survey test that assumption; they inspect         elif r.startswith('POST'):
tasks, gold solutions, and graders, never the tool implementation.            try:
We author executable contracts from each tool's advertised                         payload = json.loads('\n'.join(r.split('\n')[1:]))
surfaces (docstring, schema, prompt text, return value), check                ...
implementations and state transitions against those contracts, and            else:
trace which task verdicts depend on state a defective tool should                  session.inject({"role": "user", "content":
have written. Across four benchmarks we confirm four headline-                           "POST request accepted and executed successfully.
eligible tool-layer defect cells, from eight verified instances: three                           ..."})
demonstrated dynamically and one, MedAgentBench's Phantom
Effect, statically. Its write path is a no-op; no grader reads the         The payload is parsed into a local variable and never
record back, and where a write oracle exists at all it reconstructs     read again. No code anywhere in the repository performs
the write from the agent's own transcript, gated on a success           the write: git grep send_post_request returns noth-
string the harness fabricates. Score-at-risk bounds are basis-          ing at the pinned commit, with every requests.post
tagged rather than pooled: an exact-field evaluator puts 1,135          call site in the client-side model transport [C1, C2 
of 2,285 tasks in tau2's full generated telecom file (not its own       FINDINGS-VERIFIED.md]. Every write action in this
smaller published-evaluation subset) at risk, while a coarser           benchmark is a no-op that reports success to the agent.
whole-database-hash basis puts all 50 airline tasks at risk by
construction. Detection on the mutation arm is precise (0.867,             None of the audits we survey can see this defect. Prior audits
zero false positives over 25 flags) but recall is weak at design-       examine task instructions, gold solutions, evaluation scripts,
effect-adjusted Wilson lower bounds on real tools; an open-world        environment configurations, graders, and judges [3], [4], [5],
mutation arm gives an escape rate 0.956 on a toy domain and             [6], but all stop one layer above the tool body and treat its
an undefined rate, zero live mutants, on tau2. A pre-registered         implementation as ground truth: a grader inspecting sandbox
trajectory-replay check on ten gold trajectories exercises the          state inherits any defect in the tool that wrote it, and reports
defect zero times and records zero verdict flips.                       agreement. None of them tests the interface-to-state-transition
                                                                        contract itself.
   Index Terms--agentic benchmarks, tool calling, measurement
validity, design by contract, conformance testing, data quality            That untested contract is a data-quality problem of direct
                                                                        concern to this community. Benchmark scores are a published
                          I. INTRODUCTION                               data product that the field mines, aggregates into leaderboards,
                                                                        and consumes in model-selection and deployment decisions,
   Agentic benchmarks help decide which language models                 the context the data-quality dimensions literature judges a data
ship. A benchmark scores an agent by executing its tool                 product against [7]. This paper audits that provenance, tracing
calls against a simulated environment and reading the state             which verdicts depend on state a defective tool produced,
those calls leave behind, assuming each call changed the                the same dependency analysis provenance research formalizes
world the way its interface said. That assumption is fragile:           for a query [8]. A defect in that instrument propagates into
LiveClawBench names, as its motivating problem, that agent-             every downstream use of the score and, unlike noise, replicates
benchmark mocks are commonly "reduced to endpoint-level                 perfectly: every rerun corrupts the measurement the same way.
stubs that remove sessions, artifacts, state transitions, and
downstream side effects" [1]. This is a prospective observation            This paper tests the interface  implementation  state-
­ a reason to build higher-fidelity mocks ­ not a measurement           transition contract directly: for each mutating tool in a bench-
of which shipped benchmarks discard state transitions or what           mark, we author an executable contract from the tool's own
that costs their scores.                                                advertised surfaces (docstring, schema, prompt text, return
                                                                        strings), check the implementation and its state transitions
   The loss can be total. MedAgentBench [2] evaluates                   against it, and trace the consequence: which task verdicts
language-model agents against a simulated electronic                    depend on that state. The contributions, in order of importance:
health record. Its harness handles every write request
an agent emits in one branch of one function                               1) Score-at-risk dependency analysis: a static, per-
                                                                               benchmark bound on how many task verdicts depend
       on state a defective tool should have written, each row      decoupled from state, an effect applied without its inverse, or
       tagged with its evaluator basis (§ IV).                      state leaking across episodes: in production a fault costs an
   2) Six executable defect classes, defined as checker rules       outage; in a benchmark it costs a number nobody can tell is
       rather than prose (§ III). Phantom Effect, Partial Effect,   wrong. The layer-above pattern recurs outside this literature
       and Reset Leak have no counterpart in any surveyed           too: Kapoor et al.'s critique of agent-benchmark evaluation
       audit taxonomy.                                              practice [33], BetterBench's benchmark-quality rubric [34],
   3) A provenance-bound contract format in which every             and UTBoost's test-oracle hardening for SWE-bench [35] all
       clause cites the advertised surface it operationalizes, and  audit a layer above the tool body, none contesting this paper's
       a static plus dynamic conformance checker (§ V, § VI).       novelty. The test-state-pollution literature anticipates Reset
   4) Open-world-validated detection: precision and recall          Leak specifically: PolDet [36] and iDFlakies [37] both detect
       against both taxonomy-shaped and off-taxonomy in-            cross-test state leakage through a comparable oracle; Reset
       jected defects, under a pre-registered plan (§ VII).         Leak is mutation-only with zero field instances here, so this
   5) Confirmed findings: four headline-eligible benchmark-         precedent costs nothing beyond narrowing its novelty to target
       class defect cells across four shipped benchmarks, from      rather than technique.
       eight verified instances confirmed at a pinned commit,
       with coordinated disclosure to all maintainers (§ VIII).        The published numbers this defect sits under. MedAgent-
                                                                    Bench's paper reports "Action SR", the write-task success
   We did not invent design by contract, contract inference, or     rate, for all 12 evaluated models in its Table 3 [2]: it ranges
benchmark auditing: § II concedes the checking technique and        0.00% to 71.33%, best Gemini-1.5 Pro, two models at 0.00%.
rests the paper's novelty on the target and the consequence for     The abstract headlines 69.67% overall SR for Claude 3.5
published scores.                                                   Sonnet v2, a weighted blend of Query SR and this same
                                                                    Action SR, so the number the authors foreground inherits
   One result shapes the whole argument: MedAgentBench's            the construct-validity problem for its write half. We cite the
grader admits evidence only when followed by the literal            arXiv table, read directly; the same paper is published in
success string its own defective tool fabricates, so the tool       NEJM AI (vol. 2, iss. 9). The file containing the POST
and the grader meant to catch it are mutually consistent, and       branch (§ I) is unchanged at our pinned commit, touched
five prior audits reported nothing (§ II, § VIII).                  only once (January 2025), so every Action SR value was
                                                                    produced under an implementation in which no write occurs.
            II. BACKGROUND AND RELATED WORK                         The paper's own § 2.4.1 describes "rule-based sanity checks to
                                                                    verify the correctness of the payload of POST requests": the
   Every audit adjacent to this one stops one layer short,          transcript-reconstruction mechanism § VIII traces, gated on the
or points its contract the other way. Three families examine        fabricated success string. To be precise: the numbers are not
only the artifacts above the tool body (task instructions, gold     wrong. They faithfully measure whether the agent emitted a
solutions, evaluation scripts, environment configurations) and      well-formed POST request, not whether any clinical record
treat the tool's implementation as ground truth; a fourth applies   changed, which is what "action success rate" is taken to mean
contract vocabulary but keeps the contract as trusted input and     by anyone reading a clinical-agent leaderboard.
the agent as the system under test, the opposite direction from
this paper. Table I disposes of each by name, with the two             Why every prior audit missed it. The tool defect (Finding
sharing this project's own audited artifacts (a checklist naming    1) and the grader design (Finding 4) are mutually consis-
a tau-bench defect, and the construct-validity literature's genre   tent: the grader's gate condition is the literal success string
of judgment call) given the fullest treatment, since a reviewer     the harness injects at __init__.py:91, consumed by
would ask first whether we duplicate it. The structural point       extract_posts, so the tool reports a write that did not
is common to all: a tool that returns success without acting        happen and the grader accepts exactly that report as its
is invisible to an audit whose own artifacts remain mutually        evidence. Audited alone, each layer is coherent. The tool
consistent with each other.                                         returns what it says it returns, and the grader correctly detects
                                                                    what it looks for. BenchGuard's EVAL-MISMATCH category
   Contract inference and API oracles. The checking tech-           and Tool-Veritas's reward-basis mismatch plausibly cover the
nique here is not new. Design by contract with runtime              grader-side class of this defect, yet neither audit found this in-
verification is decades old [20], [21]; ConTract infers implicit    stance: single-layer audits are blind to cross-layer consistency
API contracts over pointer state transitions (127 developer-        defects by construction, insufficient when the layers agree with
confirmed inconsistencies) [22]; IcePICK supplies a first-          each other about a world that does not exist. We distinguish
order executable contract language [23]; AGORA+ detects             Ungrounded Oracle (§ IV) from Tool-Veritas's reward-basis
REST invariants at 80% precision [24]. Frame specifications         mismatch: theirs concerns whether the verdict matches the
formalize what a call must not change [25]; contracts have          outcome, ours the provenance of the oracle's evidence.
been synthesized for stateful modules [26] and studied for
deep-learning APIs [27]; our Ignored Argument and Partial              tau2-bench's numbers. The pinned commit sits inside
Effect detectors are metamorphic relations in all but name [28],    the code lineage producing tau2-bench's own published per-
[29], [30], and stateful sequence exploration is established for    domain pass1 results and the live public leaderboard, and both
REST APIs [31], [32]. None examines an agentic benchmark            audited defects have been present, unpatched, across that span
harness, and none of their fault models contain a success signal
                                                                    TABLE I
                                   PRIOR WORK DISPOSED OF, BY COLLISION AND DISPOSITION.

Work(s)                            Claim or apparent collision                                 Disposal

BenchGuard [3], Automated          Audit the four task artifacts (14 subcategories), a three-  Zero overlap in 27 categories [C6  GATE.md §2]; nearest miss, BenchGuard's
Benchmark Audit [4], SafeAudit     axis instruction/environment/evaluation schema, and         INST-CONTRADICT, checks instruction against gold program, both task meta-
[6]                                safety-suite coverage, respectively                         data, where ours requires reading the tool body
Tool-Veritas [5]                   Audits verdict-vs-outcome across four benchmark fam-        Audits whether the verdict matches the outcome; we audit whether the state
                                   ilies, incl. tau2-bench Retail, via execution traces;       the verdict reads was ever correctly written, so a gate reading a defective tool's
ToolGate [9], Agent Behavioral     coins "implementation-specification mismatch"               output inherits the defect regardless. The mismatch term denotes LLM-judge
Contracts [10], Contract2Tool                                                                  scoring of final answers, never a defined failure class or case study -- collision
[11], ContractBench [12]           Contract vocabulary at the agent runtime, over pre-         in name only
ToolFuzz [13]                      conditions/postconditions, runtime enforcement, tool        All four share one direction of trust -- the contract is trusted input, the agent
                                   selection, and observation-contract preservation            is under test -- and we invert it, testing the contract against the implementation
ContractGuard [14]                 Nearest engineering neighbor: tests whether produc-         that advertises it
34-fault agentic-AI taxonomy [15]  tion LangChain tools behave as their documentation          Oracle is agent-response correctness with documentation as ground truth; our
                                   promises                                                    object is a benchmark's own simulated tool, our observable the state transition,
Agentic Benchmark Checklist [16]                                                               and the documentation is precisely what we do not trust -- either side of a
                                   "Forging a tool's effects"                                  divergence may be the faulty one
Construct-validity review [19]                                                                 An adversarial-security concern, an attacker defeating a permission gate, unre-
                                   "Tool Invocation" category glossed as covering vi-          lated to the benign implementation divergence studied here
                                   olations of API contracts, mined from deployed-             Contract assumed correct, agent violates it; we study the inverse -- the tool
                                   framework issue trackers                                    violates its own contract and the victim is the measurement rather than the task
                                   Reports  -bench [17] counts empty responses as suc-
                                   cessful -- a grading-rubric defect -- inside a January      tau2-bench [18], the artifact we audit, released June 2025 after that window
                                   2024­March 2025 collection window                           closed, with a different grading design (assertion functions and database checks);
                                                                                               git log -S confirms the code carrying both our tau2-bench findings un-
                                   445 LLM benchmarks, 29 expert reviewers, pervasive          changed since the repository's first commit [EXTERNAL-VERIFICATION.md
                                   failures to map test items onto capability constructs       Task 1]. Different benchmark, codebase, and layer: theirs a ground-truth/rubric
                                                                                               defect, ours tool-implementation defects
                                                                                               Our evidence sits outside that genre of judgment call: a tool either honors its
                                                                                               declared contract on a given call or it does not, the checker rule is executable,
                                                                                               the witness a pair of state snapshots

[18], [EXTERNAL-VERIFICATION.md Task 2]. We claim                   post, args, result): the canonical state snapshots be-
no causal link from either defect to any specific published         fore and after a call, the call's arguments, and its return value.
number: whether a task verdict depends on the affected state        The classes are typed at the tool boundary. Prose descriptions
is exactly what § IV's score-at-risk analysis answers, and          are glosses; the rule is the definition.
asserting it from outside that analysis would overreach.
                                                                       Four of the six classes are field-observed in shipped bench-
   The honest summary of this section: the checking technique       marks (§ VIII). Invariant Break and Reset Leak have no
is mature SE, the audit target is unoccupied, and the field's       field-observed instance anywhere in our findings: they exist
own newest benchmark work names our symptom while                   only as injected mutants, and every count in § VII and § VIII
auditing nothing [1]. What is new here is the target, the defect    keeps that distinction visible. We note the direction of travel
classes that target implies, the cross-layer blindness result, and  rather than hide it: Ignored Argument was itself mutation-only
the traced consequence for published scores.                        until the third and fourth benchmarks were audited, and it
                                                                    is now the most common class in the finding set, with four
III. THE BENCHMARK TOOL LAYER AS A MEASUREMENT                      instances.
                             INSTRUMENT
                                                                       Result­state disagreement is a signal, not a class. The
   A benchmark tool is not application code that happens            Ignored Argument rule is deliberately state-only. An earlier
to be simulated. It is the write path of a measuring device.        draft required both post-state and result to be invariant under
When production software has a defective write path, a service      the argument, and that conjunctive rule fails on a real case:
degrades and someone files an issue; when a benchmark               AgentDojo's reserve_car_rental discards end_time
tool has one, a published number is corrupted silently and          from state but interpolates it into the success string, so the
reproducibly, and keeps being cited either way. This section        result varies with the argument and the conjunctive rule would
fixes the object under test, the defect classes, and the principle  never fire (§ VIII, Finding 6), missing one of the checker's
separating a defect from a legitimate simplification.               own anchor findings. The rule is therefore state-only, and a
                                                                    result that varies with an argument whose effect on state is
   Why the defect is invisible downstream. Every determin-          absent is reported as an aggravating signal on the finding it
istic evaluator in the surveyed literature reads state that tools   accompanies.
wrote (tau2-bench's own evaluator compares final database
state between gold and predicted runs [C11]), so a defective           The benign-simplification principle. The strongest ob-
tool's output is inherited by any gate reading it (§ I, § II).      jection a maintainer can raise is that these are intentional
Consistency between a broken tool and a grader reading its          simplifications of a simulation that never claimed fidelity. The
output is not validity; that is why the tool layer needs its own    answer is a criterion, stated once and applied uniformly:
audit rather than better downstream checks.
                                                                          A simplification is benign exactly when it is adver-
   The defect taxonomy. Table II defines six defect classes,              tised. The defect is never the simplification; it is the
each as an executable checker rule over the tuple (pre,
Class                                                                    TABLE II                     Status
                                                        SIX EXECUTABLE DEFECT CLASSES.
Phantom Effect                                                                                        field-observed
Unenforced Precondition  Checker rule (over pre, post, args, result)                                  field-observed
Ignored Argument                                                                                      field-observed
Partial Effect           success signal true  advertised effect delta absent                          field-observed
Invariant Break          precondition predicate false  (no error signal  state mutated)               mutation-only
Reset Leak               post-state invariant under variation of an argument advertised as effective  mutation-only
                          1 advertised effect predicate holds   1 fails on the same call
                         environment invariant false after a legal call sequence
                         snapshot after reset = initial snapshot

      undisclosed divergence between what the interface             is checked before it is trusted, not merely reproducible in
      tells the agent and what the implementation does.             principle.

   Three consequences follow. First, tau2-bench's own main-            Step 1, defect to field. From a contract's VIOLATES
tainers already draw this line: at airline/tools.py:689             clause, the predicate's own compiled AST (never the pred-
they advertise a deferred flight-database update in the surface     icate's source string) yields the state paths the tool should
the behavior is visible from, and the checker correctly does        have written and did not, or wrote unconditionally when a
not flag it, while at line 367 they merely log that seat release    precondition should have blocked the write. Step 2, field to
is not implemented, in a warning the agent never sees, and          evaluator. The benchmark's own evaluation code is parsed the
the checker flags it. The distinction between disclosed and         same way, by AST rather than by pattern match, for the state
undisclosed divergence is theirs before it is ours. Second,         paths it reads, and each read site is classified by provenance:
either side of a divergence may be repaired: a maintainer who       live simulator state, or the agent's own transcript. Step 3,
fixes an aspirational docstring restores conformance exactly        evaluator to tasks. Every task whose verdict depends on a
as completely as one who fixes the code, and § VIII counts          field in the Step 1/Step 2 intersection is enumerated from the
such a response as a resolved finding, converting the audit         benchmark's own pinned task-definition file, exhaustively.
from an accusation into a specification of what disclosure
would make a simulation honest. Third, the advertised surface          Every at-risk figure carries a basis tag, and
is the right baseline against the position that a benchmark         the tags are never pooled. tau2's airline domain
owes fidelity only to itself, because the agent's behavior is       grades cancel_reservation by comparing a full
the measured quantity and is conditioned on the interface           database hash between the gold and predicted runs
text. That baseline is scoped to one audience: agent-side           (toolkit.py:242-244); any field a defective tool writes
measurement validity, audited throughout; it says nothing           is, by construction, read by that mechanism, so all 50 airline
about consumer-side label validity, whether the leaderboard         tasks in the affected reward basis come back at risk. That is a
reader, who sees no docstring, draws the right conclusion           measurement of the evaluator's coarseness, not of the defect's
from a published score. The line-689 update is a true negative      reach, and Table III tags it whole_state_hash rather
because the agent sees it; whether any score consumer does          than let it stand next to a sharper number. Telecom grades
is untouched here except where MedAgentBench's construct-           refuel_data through per-task assertion functions instead,
validity result forces it (§ II).                                   and the intersection is exact: 1,135 of 2,285 tasks read a field
                                                                    the defective precondition lets through unconditionally, 1,120
   One evaluator-layer property, kept outside the taxon-            of them on the specific written attribute (exact_field)
omy. MedAgentBench's write graders exhibit a property we            and 15 more on the containing collection alone
name Ungrounded Oracle: the evaluator's oracle for a state          (collection_only). The 2,285 is the full generated task
change reads the actor's claim rather than the state. It is field-  file (data/tau2/domains/telecom/tasks.json),
observed in § II, but it is not a seventh class: the six classes    not the roughly 114-task subset tau2-bench's paper evaluates;
are typed over (pre, post, args, result) at the tool                no mechanism here reconstructs that historical subset, so a
boundary, while oracle grounding is typed over grader source        published-evaluation-scoped count is not computed. A single
code, a different object entirely. § IV emits it as a per-task      benchmark carries both bases on different tools, and no
classification of the benchmark's evaluator rather than as a        project-wide percentage is stated across them: only per-basis
row in Table II.                                                    counts, each with its own denominator.

        IV. SCORE-AT-RISK DEPENDENCY ANALYSIS                          The bound is a bound, in one direction only. At-risk is
                                                                    an over-approximation of misgrading via the state-dependency
   The paper's central deliverable is not a violation count         mechanism, and it holds only where the oracle is state-
but a trace: given a confirmed defect, which task verdicts          grounded. It says a task's verdict could have been computed
could have been computed on state that defect corrupted.            wrong because its evaluator reads a field the defective tool
analysis/score_at_risk.py performs the trace as                     controls, not that the verdict was wrong, and nothing about
three static steps, no model anywhere in the pipeline, over the     failure modes outside that mechanism. § VII checks whether
1,208 rows written to report/score_at_risk.jsonl.                   the bound is vacuous on a drawn sample and finds that, on
A --verify flag re-derives every row from the same pinned           the trajectories it recorded, it is not exercised.
inputs and diffs it against the file on disk, so the trace
                                                                       Oracle grounding is a first-class output, not a footnote,
                                                            TABLE III
                             SCORE-AT-RISK BY DEFECT, WITH BASIS AND GROUNDING.

Benchmark : tool             Defect class          Basis                        At risk / total Experiment Oracle grounding
                                                                                                       frame

tau2 airline : cancel_       Partial Effect        whole_state_hash             50 / 50      7         state grounded
reservation                  Unenforced Precondi-                               1135 / 2285  1120      state grounded
tau2 telecom : refuel_data   tion                  exact_field (1120)
                             Ignored Argument      + collection_only            1 / 16       4         state grounded
AgentDojo banking : update_  Ignored Argument      (15)                         1 / 20       0         mixed
scheduled_transaction        Phantom Effect        exact_field                  undefined    n/a       60 transcript / 90 mixed / 150 no-oracle /
AgentDojo travel : reserve_  Ignored Argument                                   not_         not com-  0 state (of 300)
car_rental                                         exact_field                  computable_  putable   not computable
MedAgentBench : post-write                                                      appworld_
                                                   transcript (not a state ba-  unreachable
MM-ToolSandbox : venmo_                            sis)
social                                             not computable

because a naive field-intersection returns the wrong answer          the gold-invocation rule. Argument-specific defects make the
for MedAgentBench, in the wrong direction. Its write                 two rules select different, non-nested populations: field de-
graders reconstruct the intended write from the agent's own          pendency cannot see which argument path a task exercises,
transcript, gated on the fabricated success string of Finding        gold-invocation cannot see which field the oracle reads, and
1 (§ I, § VIII). Field intersection alone cannot see this: the       both populations are reported with their own denominators.
grader-checked field and the field the tool should have written
share the same name, so a names-only rule would call nearly             MM-ToolSandbox reports what it can compute and
every one of the 300 cases at risk. The real classification          nothing it cannot. Step 1 identifies the sort_by
is computed per grader function from whether it calls the            field venmo_social drops (Finding 7). Steps 2 and
transcript-reconstruction routine, the live-state routine, both, or  3 do not run: the AppWorld-tier scenarios that exer-
neither: of 300 write-tagged cases, 60 are graded uncondition-       cise this tool are graded inside the appworld pack-
ally from the transcript, 90 read live state only to gate whether    age, which this project cannot clone. Table III's row
a write is required and then grade the write itself from the         reads not_computable_appworld_unreachable, not
transcript, 150 have no write oracle at all, and zero are graded     a zero and not an omission. A null population is data about
by reading the write back from FHIR state. Score-at-risk is          what a static, offline audit can reach, not a claim that no task
undefined for the 60 and indeterminate for the 90, never zero,       is at risk.
never inflated, a distinct verdict emitted by name: "oracle not
state-grounded." The evaluator property is Ungrounded Oracle                        V. THE CONTRACT SPECIFICATION
(§ III); this classification is what makes it a result rather than
an aside.                                                               Every contract clause cites the provenance of the advertised
                                                                     surface it operationalizes (file, line, and a verbatim quote
   The experiment frame need not nest inside the at-                 checked against the pinned commit at build time) across three
risk population, and for Finding 5 the two sets are                  grounding tiers: agent-visible text (docstring, schema, prompt
disjoint. The natural expectation (that a stricter selection         template, tool return, README, external standard) the agent
rule, a task whose own gold solution invokes the defective           was conditioned on; maintainer-annotated text (a comment,
tool, is always a subset of the field-dependency population)         log line, or TODO) stating intended semantics no agent ever
is wrong: experiment_frame_subset_of_at_risk                         sees; and inferred semantics we assigned ourselves. Only
is computed, not asserted, and it is False for Agent-                agent-visible clauses are headline-eligible (a violation there
Dojo's Finding 5. update_scheduled_transaction                       corrupts the measurement, § III), while the other two tiers
is broken on every argument at falsy values (amount                  are reported separately, never folded into a headline count:
at 0, date/subject/recipient at the empty string,                    the paper's construct-validity firewall against the "just our
recurring at False), but recurring is the one ar-                    opinions" objection. The benign-simplification principle of
gument for which a falsy value is a legitimate advertised            § III governs contract authoring too: either side of a divergence
input, which is why it alone carries task risk here: four tasks'     may be repaired, a documentation fix resolving a finding
gold solutions call the tool but only to vary amount or              exactly as completely as a code fix (§ VIII). The checker
recipient, so none of them lands in the at-risk set field            also knows when not to fire: tau2-bench's advertised deferred
dependency computes. The two sets share zero tasks. The one          flight-database update and its advertised non-enforcement of
task whose oracle does read .recurring, UserTask6,                   cancellation eligibility are both true negatives our test suite
reaches that field through a different, non-defective tool,          exercises, the credibility anchor behind every VIOLATES
schedule_transaction, and is therefore at risk by the                verdict elsewhere in the paper.
field-dependency rule and outside the experiment frame by
                                                                        Contract authoring is measured, not trusted, but the mea-
                                                                     surement is of a narrower thing than independent replica-
tion. Both annotator sets, A and B, are agent sessions on                                            TABLE IV
the same underlying model family, and only B authored             REAL-TOOLS CLOSED-WORLD RECALL BY MUTATION OPERATOR.
blind. A already knew which tools carried confirmed find-
ings; what follows is evidence of stability of sentence-          Operator      n   eff.   de-    missed  untest-  error     Recall
to-predicate translation under one-way blindness, not of                     drawn   n    tected           able           (Wilson LB)
independent human judgment, and the paper claims no more
than that. The two sets are compared extensionally against        M-PHANTOM 7       7     5       0       0        2 0.359
a shared, pre-committed probe corpus across six tools, over
comparable clause pairs on successful calls only. The blind       M-PRECOND 7       5     1       6       0        0 0.020
stratum agrees on 98 of 98 probe comparisons across 16
clause pairs, a span covering four of the five blind tools;       M-IGNARG   21 15.13 4           12      1        4 0.066
the fifth, venmo_social, contributes zero comparable pairs,
because the blind annotator segmented the multiplexed tool        M-PARTIAL  8      6     1       7       0        0 0.018
into disjoint dispatch branches and never declared Finding 7's
own anchor arg.sort_by. That anchor is incomparable,              M-INVAR    8      8     0       8       0        0 0.000
not agreeing, itself a segmentation result about multiplexed
tools. Four tools (refuel_data, cancel_reservation,               M-RESET    n/a n/a n/a n/a n/a n/a no data
update_scheduled_transaction, venmo_social)
needed hand-built witness probes, disclosed before any out-       Eff. n is the tool-clustered effective sample size the design-effect adjustment
come was inspected, because the frozen corpus could not
reach them. A sixth tool, cancel_reservation, forms               computes from n drawn (§ 7 of the plan); it, not n drawn, feeds every
a contaminated stratum (Annotator B had read a worked
example of this exact tool beforehand) and is reported sep-                         Wilson bound above.
arately, never pooled with the blind result. One finding
flips within the blind stratum: reserve_car_rental's              cost of one disclosed refreeze cycle (checker-freeze-v1
arg.end_time is VIOLATES under A and UNTESTABLE                   to checker-freeze-v2). Adapter mechanics, the subpro-
under B, because A's contract declares a probe_values             cess boundary, and the remaining frame-path expressiveness
hint pinning the argument to parseable ISO strings and B's        gap are in the artifact.
does not, so every probe B's contract can build fails inside
datetime.fromisoformat() before reaching the tool's                                  VII. DETECTOR VALIDATION
effect code, a missing annotation, not a disagreement about
advertised semantics; both annotators independently flagged          The pre-registered plan fixes, before any mutant
the underlying defect in their own notes. The full grammar,       existed, what each arm can and cannot license
clause-authoring protocol, segmentation rules, and worked-        (experiments/detector_analysis_plan.md,
example detail are in the artifact.                               committed at tag checker-freeze-v1, commit
                                                                  57b019d). The closed-world arm asks whether the checker
   AI-use disclosure. Both annotator sets above, and the          detects a defect of a class we authored; a good result licenses
numeric analyses of § IV, § VI, and § VII, were produced          the sentence "the checker detects violations of the clauses we
by agent-run scripts and sessions, disclosed here per IEEE's      wrote," never "the checker detects tool-contract defects." The
policy on generated content.                                      open-world arm asks whether the checker notices a defect
                                                                  nobody designed for its taxonomy; a good result licenses
               VI. THE CONFORMANCE CHECKER                        only a lower bound on the taxonomy's incompleteness,
                                                                  never a claim of coverage. Every rate below is stated at
   The checker has a static half (five AST-only checks that       its design-effect-adjusted Wilson lower bound (Kish design
flag candidate sites for a person to read) and a dynamic half     effect, Rao-Scott one-way ANOVA ICC estimator, per § 5
that produces verdicts: snapshot a fresh environment, invoke      of the plan) with its effective denominator printed beside it.
the tool, snapshot again, and evaluate every clause against the   Toy-domain numbers, from the domain the checker's own
resulting (pre, post, args, result) tuple, resolving              authors wrote with this taxonomy in mind, are reported
to CONFORMS, VIOLATES with a minimal witness, or                  for debugging and never enter a headline; the headline is
UNTESTABLE with a reason code; UNTESTABLE stays in                real-tools recall only, drawn from tau2-bench, AgentDojo,
every denominator this paper reports. tau2-bench, AgentDojo,      and MM-ToolSandbox.
and MM-ToolSandbox run through adapters as subprocesses;
MedAgentBench has no adapter and is checked statically (§ I).        Real-tools closed-world recall.
All six defect classes now have a real dynamic or static code        Recall as shipped is low, and a post hoc
path, but four of them did not until a post-freeze mutation       diagnostic written after the corpus was scored,
sweep exposed the gap. Reset Leak and Invariant Break             experiments/decompose_closed_world_misses.py,
verdicts, 35 frame clauses, and 17 of 19 tau2 contracts were      traces why: of all 33 real-tool misses, the taxonomy itself
silently never dynamically exercised. All four are fixed, at the  owns 2 (clause_gap), the frozen probe corpus owns
                                                                  29 (probe_corpus_unreachable, never driving the
                                                                  mutated path into an observable state), and the remaining
                                                                  2 are probe_gap and target_not_found. The
                                                                  bottleneck sits in probe reachability, not in the six defect
                                                                  classes; the per-operator sub-breakdown and the watchdog-
                                                                  timeout exchangeability question are in the artifact. M-
                                                                  INVAR's 0.000, decomposed by liveness screening only
                                                                  (operators_liveness_only) rather than the full three-
                                                                  way classifier, reads as "no probe ever reached a mutated
                                                                  invariant path," not "the checker cannot detect invariant
                                                                  breaks." M-RESET has no real-tool site to draw at all: no
                                                                  tool-implementation file in any of the three real benchmarks
                                                                  defines a state-restoring function, so the honest report is "no
data," not a rate of zero. Biconditional success (a contract          closed-world numbers above.
clause asserting success only if every advertised effect holds,          tau2-telecom produced no usable escape rate. cosmic-
the mechanism M-PHANTOM's detector depends on) is
opt-in per contract, adopted on 18 of 19 tau2-bench, 1 of 7           ray scored 465 mutations across 2,325 (mutation, tool) pairs
AgentDojo, and 0 of 5 MM-ToolSandbox contracts; where                 against the domain's five non-anchor tools, and zero were
not adopted, Phantom Effect cannot fire at all: a second, more        behaviourally live, so the escape-rate denominator is zero. The
visible reason M-PHANTOM's recall sits at 0.359 rather than           cause is the probe corpus, not the detector: its recorded-real-
higher.                                                               call component has no artifact anywhere in the project, and
                                                                      the two components that do exist are type-generic placeholders
   Precision. Zero false positives across every flag the checker      that fail existence checks on tau2's entity-keyed tools and raise
raised against an injected mutant in the closed-world arm,            before any mutation could show (detail in the artifact). A
on a pooled toy-plus-real sample of 25 flags (TP=25, FP=0;            zero denominator is not evidence of a well-covered taxon-
24 semantics-preserving controls scored alongside): precision         omy; this arm is reported as an open question, not a result.
0.867 at the Wilson lower bound. This denominator is                  A precompute cache predating checker-freeze-v2 had
mutation-arm flags only; it says nothing about the field find-        already produced a more favorable v1 run on this arm (281
ings of § VIII, which were confirmed by direct source reading,        of 465 scored, 33 live, escape rate 0.325); it is not reported
not by this count. This is the strongest number in the section        as a result, since whether those 33 live mutants were genuine
and licenses nothing about the recall figures above: a detector       or stale-cache artifacts is now unresolvable, and the run is
that never lies about what it catches can still catch very little of  disclosed rather than dropped in the direction that would have
what it should. For context, AGORA+ reports 80% precision             flattered us.
on REST invariant detection [24]; this checker's precision sits
above that figure, though its design structurally cannot false-          Evaluation impact: a null, not a gap. Two tau2-bench find-
positive against a clause it never wrote, an asymmetry the            ings carry mechanically verified one-hunk patches: cancelling
comparison does not correct for.                                      a reservation leaves seats_after at 0 unpatched and re-
                                                                      stores it to 3 patched; refuelling a suspended line succeeds
   Achieved N against pre-registered N. Only M-PRECOND                unpatched and raises ValueError patched. Ten trajectories
and M-IGNARG met the 25-per-class target, counting real               replayed from each finding's own gold reference solution (five
and toy sites together as the pre-registration's draw target does     per finding, seed 1, zero discarded for infrastructure failure)
(the recall table above, by contrast, reports real-tool sites only,   all land in a single outcome cell: at risk, not exercised. Zero
which is why its own n-drawn column reads 7 and 21 for these          trajectories trip either finding's trigger predicate, and zero
same two operators rather than 25). The rest are the plan's           verdicts flip. For F3 (airline cancel_reservation), no
§ 8 no-reuse refreeze rule meeting a structurally small pool,         flip was possible by construction: the evaluator compares gold-
not a scoring shortfall: each remaining operator's real-tool site     run state against agent-run state, and both sides execute the
pool was small enough that the first freeze already drew most         identical, still-defective tool, so the symmetric-oracle structure
or all of it, and the second freeze either drew what was left         absorbs the defect regardless of the exercise rate. For F2 (tele-
or, for M-INVAR and M-RESET, fell back to rescoring the               com refuel_data), a flip was possible in principle and did
first freeze's own sites against the second freeze's checker,         not occur, because none of the five sampled gold trajectories
the one documented exception to the no-reuse rule. The full           calls the tool on an inactive line. Per the pre-registered inter-
per-operator pool table is in the artifact.                           pretation (experiments/analysis_plan.md), this is a
                                                                      finding about conditional validity, not a null result: the score
   One refreeze cycle. The numbers above are scored under             is correct today because no agent happens to take the broken
checker-freeze-v2 (commit 5824376), not the first                     path, and nothing in the benchmark prevents one from doing
freeze: a mutation sweep against checker-freeze-v1                    so. N=5 per finding is provisional. F3's affected population of
found four taxonomy claims the checker had been carrying              7 tasks makes it near-exhaustive; F2's population of 1,120 does
as closed with no code behind them, detailed in § VI, and the         not (§ IX). The null therefore cannot separate a defect latent
checker was refrozen before any number here was scored.               under every gold trajectory from one an undersized sample
                                                                      missed. The experiment design, the full per-finding outcome-
   Open-world escape rate, toy. cosmic-ray scored 368 mu-             cell table, and the evaluator-basis exercise/flip breakdown are
tations across 4,048 (mutation, tool) pairs; 229 were be-             in the deposited artifact.
haviourally live, and the checker missed a Wilson lower
bound of 0.956 of them (point estimate 0.983), decomposed                  VIII. FINDINGS AND COORDINATED DISCLOSURE
as 119 of 225 escapes tracing to a clause the taxonomy never
wrote, 86 to a clause whose probes never drove the tool into             Across four shipped benchmarks, the ledger's twelve VI-
the exposing state, and 20 to the checker crashing before             OLATES clause rows collapse to eight confirmed finding
evaluation. Only the mechanical declared-volatile-field rule ran      instances, populating seven benchmark-class cells; the unad-
in this automated pass (the interface-invisibility and second-        judicated suspend_line candidate (below) adds an eighth,
annotator rules need a human annotator and are in the artifact),      matching Table VI's Classes total. The compressed count is
making 0.956 conservative toward overstating the checker's            deliberate: this project's reporting rule counts unique defect
blindness. Per the committed framing, this is a lower bound           classes per benchmark, because eight bugs in one copied
on the taxonomy's incompleteness, not evidence against the
helper is one bug. Four of the eight cells cannot carry a           missing a Wilson lower bound of 0.956 of behaviourally live
headline: tau2-bench's Partial Effect is maintainer-annotated (a    mutants, decomposed there into unwritten-clause, unreached-
logger.warning the agent never sees), not agent-visible; Med-       state, and checker-crash causes. Six classes, even perfectly
AgentBench's Ungrounded Oracle is an evaluator property,            detected on the defects they name, would still miss most of
not one of the six tool-layer classes; AgentDojo's Phantom          what an operator with no knowledge of the taxonomy can
Effect cell is real in the contract but unconfirmed in practice:    break, a limitation stated in the paper's own numbers, not
its biconditional re-tag needs recurring and amount to              inferred from outside them.
violate on the same call, which the banking fixture never
does; and suspend_line is itself unadjudicated, not one                The equivalent real-benchmark run produced no usable
of the eight confirmed instances. All four are reported in          number at all, and the absence is reported rather than
full, labelled, and excluded, giving eight cells, of which four     hidden (§ VII). cosmic-ray's 465 mutations against tau2-
are headline-eligible. Four of the eight confirmed instances        telecom's five non-anchor tools yielded zero behaviourally live
are Ignored Argument. Every finding was verified by direct          mutants, so the escape-rate denominator is zero; the cause is
source reading at a pinned commit, independent of whoever           the probe corpus, not the checker. The same missing artifact
first surfaced it, with a clean-clone reproduction command in       (no production caller ever supplies a recorded real call) also
the artifact. Per-benchmark totals appear in Table VI.              explains the reserve_car_rental agreement flip (§ V)
                                                                    and 29 of § VII's 33 real-tool misses, which makes a recorded-
   MedAgentBench (Findings 1 and 4) is the paper's central          real-calls corpus this project's single highest-value piece of
chain. Finding 1 is the no-op POST branch quoted in § I. Find-      future work.
ing 4 makes it undetectable: the grading module reconstructs
each write from the transcript, admitting evidence only when it        Sample sizes fall short of the pre-registration for four of
matches Finding 1's fabricated success string (§ II). No grader     six mutation operators, and the shortfall is structural, not
ever issues a FHIR read to verify the write. Patching the tool to   a scoring choice. § VII's achieved-N table shows only M-
perform real writes would change no score, because no grader        PRECOND and M-IGNARG meeting the 25-real-tool-site
observes the state a patch would repair ­ why § VII attempts no     target; the rest exhausted a structurally small corpus under
A/B experiment here. Finding 4's evidence file, refsol.py,          the plan's § 8 no-reuse refreeze rule. Achieved N is reported
is absent from the repository; MedAgentBench's README               beside the pre-registered target in every case, never silently
points to an unhashed, unversioned Stanford Medicine Box            absorbed into a rate.
share for it. We pin a copy in the artifact at its SHA-256 hash
(repro/env/refsol.py), letting a reader verify § IV's                  M-RESET has no real-tool site at all (§ VII): reset logic
60/90/150/0 classification against it, not our transcription.       lives in the adapter layer, which reconstructs environments
                                                                    wholesale rather than repairing them in place, so only the
   Two classes recur across the other three benchmarks: an          synthetic toy domain colocates reset with its tools for the
interface tells the agent a call has an effect, and the imple-      operator to enumerate. The honest report is "no data," not a
mentation either drops it silently (Ignored Argument, four          recall of zero, and Reset Leak's field-observed count anywhere
of eight instances) or reports success regardless (Phantom          in this project's findings is zero.
Effect, stacked on Finding 5). A single-layer read finds nothing
wrong: each layer is internally consistent with itself.                The subprocess boundary absorbs exceptions the
                                                                    checker never sees, and whether that matters is unverified.
   All four benchmarks are cited at the audited commits             A toy-domain bug during the closed-world v2 run showed the
[2], [18], [38], [39]; instance-level evidence is in the find-      mechanism directly: an adapter's invoke() caught only its
ings ledger, reproducible from clean clones via the artifact's      own declared error type, so a probe past a deleted guard drove
shipped commands.                                                   the tool into code assuming the check held, and the result-
                                                                    ing raw exception aborted the mutant rather than registering
   Coordinated disclosure. We disclose all findings to the          as a violation. The same subprocess boundary sits between
four maintainer teams on 2026-09-10, seventeen days before          the checker and every real adapter and swallows arbitrary
submission, with per-finding reproduction commands and pro-         exceptions before the harness can classify them; whether an
posed repairs. Under § III's principle, a repair to either side of  absorbed crash on a real tool would score as detected or
a divergence resolves a finding, as does a documentation fix.       silently missed has not been checked, which bounds what
Any response received by submission is reported here with its       every real-tools recall number in § VII means.
substance; for a team that has not responded by then, the fact
and date of non-response is reported neutrally, with no infer-         The agent-experiment sample is provisional at N=5 per
ence drawn from silence; and a finding a maintainer contests        finding (§ VII), and a null at that size proves less than it
is marked contested in the camera-ready copy, with the main-        looks like it proves. F3's affected population of 7 tasks makes
tainer's reasoning stated: [N12: report/disclosure log.md].         5 close to exhaustive; F2's population of 1,120 does not. Zero
                                                                    of ten trajectories exercising either trigger predicate therefore
                    IX. THREATS TO VALIDITY                         cannot separate a defect latent under every gold trajectory from
                                                                    one an undersized sample missed.
   The taxonomy is not the space of possible defects, and
the open-world arm measures the gap rather than closing                Benchmark selection is anchor-driven, not a sample
it. § VII's open-world arm on the toy domain found the checker      of a population. tau2-bench and MedAgentBench entered
                                                                    this study because their defects were already known before
                                                                            TABLE V
                                      CONFIRMED FINDINGS (INSTANCE LEVEL), WITH QUOTED EVIDENCE.

# Bench. @ commit Tool : line             Class                  Tier                                           Quoted evidence and disposition

1 MedAgentBench POST branch, __init__. Phantom Effect            agent-visible                                  Fabricated return: "POST request accepted and executed successfully";
                                                                 (prompt_                                       payload parsed, never re-read. Chain continues below.
@ 9926011          py:85-91                                      template,

                                                                                                  tool_return)  Docstring: "Line status must be Active." Check present in source,
2 tau2-bench @ refuel_data, telecom Unenforced Precondi- agent-visible

c3398666           /tools.py:607-657 tion                        (docstring)                                    commented out; sibling gb_amount check survives -- a selective, not

                                                                                                                absent, omission.

3 tau2-bench @ cancel_reservation, Partial Effect                maintainer-                                    Log line: "Seats release not implemented for cancellation!!!" (367),

c3398666           airline/tools.py:                             annotated: not TODO at 689. Seats never restored; reset confirmed per-episode (Reset

                   315, 363-368                                  headline-eligible Leak label withdrawn).

4 MedAgentBench write graders, refsol.py Ungrounded Oracle grader source                                        Gate string "POST request accepted" consumed by extract_posts;

@ 9926011          (SHA-256-pinned; not in (evaluator property,                                                 60/300 cases graded unconditionally from transcript, 90 more gate on
                                                                                                                live state but still grade from transcript.
5 AgentDojo        repo)                  § III)                                                                recurring: bool | None behind a truthiness guard: settable
              @ update_scheduled_         Ignored Argument + agent-visible                                      True, never False. Returns "Transaction ... updated." unconditionally.
                                                                                                                Sibling update_user_info repeats the guard on four string fields
089ed46            transaction,           Phantom Effect         (docstring)

                   banking_client.py:

                   115-151                                                                                      without the phantom signal -- same class, Finding 5's count, not a ninth

6 AgentDojo @ reserve_car_rental, Ignored Argument agent-visible                                                instance.
                                                                                                                end_time discarded from state but interpolated into the success mes-

089ed46            travel_booking_                               (docstring)                                    sage (the aggravating signal of § III). No shipped task's ground truth

7 MM-              client.py:382-400                             agent-visible                                  exercises this tool -- excluded from any score-impact claim.
                   venmo_social, mini Ignored Argument                                                          sort_by documented twice in the docstring, never forwarded in the

ToolSandbox        /venmo.py:464-470                             (docstring)                                    comment-listing branch; the correct forwarding pattern appears 250 lines

     @ 1e8e932                            Ignored Argument agent-visible                                        above in the same file.
8 AgentDojo @ invite_user_to_                                                                                   Docstring: invite "should be sent" to user_email; body never reads

089ed46            slack,      slack.py:                         (docstring)                                    the mandatory parameter. Surfaced by the static checker's full-repository
                   93-103                                                                                       scan before a human confirmed it.

A third tau2-bench candidate stays below the reporting threshold: suspend_line's arg.reason VIOLATES in the ledger at the inferred tier. A

required reason is logged and never persisted, but whether the docstring advertises persistence rather than a log entry is contested and unadjudicated; not

                      counted among the eight confirmed instances, though it is the eighth cell in Table VI's ledger total.

                                     TABLE VI                                 frame mechanism, named here rather than hidden.
PER-BENCHMARK AUDIT TOTALS. CLASSES COUNTS EVERY LEDGER                          Repository history cannot support every ordering claim

    CELL, CONFIRMED INSTANCES AND THE ONE UNADJUDICATED                       this paper might like to make. git init ran on 2026-
CANDIDATE ALIKE; THE PARENTHETICAL IS THE HEADLINE-ELIGIBLE                   08-21, after most of the design work already existed, and the
                                                                              first commit imported the project as a single tree. A squashed
                                       SUBSET.                                import cannot demonstrate any pre-registration preceded the
                                                                              design decisions it was meant to freeze, and the paper does not
       Benchmark       Tools Mut. Impl. Classes (HL)                          claim it does. What remains verifiable, and what the paper re-
                                                                              lies on, is narrower: checker-freeze-v1 predates the first
       MedAgentBench           33      1         2 (1)                        scored mutant, and experiments/analysis_plan.md
                                                 3 (1)                        predates the first recorded trajectory, both checkable from
       tau2-bench              19 19   3         2 (1)                        commits made after the import. The distinction is stated here
                                                 1 (1)                        rather than left for a reviewer to discover (PROVENANCE.md).
       AgentDojo               77      3
                                                                                 One refreeze cycle is disclosed rather than folded into the
       MM-ToolSandbox          55      1                                      final numbers. § VI details the four taxonomy claims a mu-
                                                                              tation sweep against checker-freeze-v1 found uncoded,
       Total                   34 34   8         8 (4)                        all fixed before checker-freeze-v2 scored any number
                                                                              in § VII. A framework that reports the sweep that found its
Excluded (reported, not headline): MedAgentBench Ungrounded Oracle            own holes earns more trust than one presented as complete
                                                                              from the start; the cost, one refreeze cycle rather than zero, is
(evaluator-layer, not tool-layer); tau2-bench Ignored Argument                stated plainly rather than absorbed into the total.

(unadjudicated candidate); tau2-bench Partial Effect (maintainer-annotated);                             X. CONCLUSION

AgentDojo Phantom Effect (fixture never trips biconditional).                    This paper tested a layer no published benchmark audit
                                                                              reaches: the tool implementation that produces the state a
the four-benchmark protocol existed; AgentDojo and MM-                        grader reads. Four benchmarks yielded eight verified instances
ToolSandbox were added afterward to test whether the tax-                     across six executable defect classes, four headline-eligible
onomy generalized beyond the two anchors. No prevalence                       under a grounding rule that excludes what no agent ever saw.
claim, what fraction of shipped benchmarks carry a given                      The score-at-risk analysis traced what those defects could
defect class, or how common Ignored Argument is generally,                    have cost in published verdicts, tagged by evaluator basis
is supportable from four benchmarks chosen this way. The                      rather than pooled into one number, and the open-world arm
four findings sets are existence proofs that the defect classes               measured how much of the defect space six classes still
occur in shipped, audited artifacts, not a survey of how often                miss. None of this establishes how common these defects
they occur.

   The frame-path grammar cannot express one of its own
most natural clauses. spec/GRAMMAR-GAPS.md records
that no wildcard segment can exclude a single key, so a frame
clause meaning "no other reservation is modified" cannot
be written without also colliding with the tool's own adver-
tised effect on the one reservation it is supposed to change.
The shipped cancel_reservation contract substitutes a
weaker, fully expressible claim about an unrelated collection;
the distance between the two is a real coverage hole in the
are across agentic benchmarks generally: four anchor-driven                        [16] Zhu, Jin, Pruksachatkun et al., "Establishing best practices for building
audits are existence proofs, not a survey. Benchmark scores                              rigorous agentic benchmarks," 2025, v5. Preprint. Referred to in text as
are published measurement data the field mines and reuses in                             the Agentic Benchmark Checklist.
leaderboards and model-selection decisions, and an instrument
defect at this layer propagates into every downstream use                          [17] Yao, Shinn, Razavi, and Narasimhan, " -bench: A benchmark for tool-
of the number it produced. What this checker demonstrably                                agent-user interaction in real-world domains," 2024, preprint; distinct
delivers today is high-precision flagging, zero false positives                          artifact and codebase from  2-bench [18] (§ II).
across 25 mutation-arm flags, paired with a score-at-risk trace
from a confirmed defect to the verdicts it could corrupt;                          [18] Barres, Dong, Ray, Si, and Narasimhan, " 2-bench: Evaluating conver-
coverage remains the open problem § IX quantifies. A score                               sational agents in a dual-control environment," 2025, preprint.
consumer should read an at-risk bound as a due-diligence flag,
never a correction to the published score. Maintainers can act                     [19] Bean, Kearns, Romanou et al., "Measuring what matters: Construct va-
now: run a precision-favoring check like this one against the                            lidity in large language model benchmarks," 2025, self-listed as NeurIPS
tool layer itself, not only tasks and gold solutions, and treat                          2025 Datasets and Benchmarks Track; not independently confirmed.
a flag as cause to look, not to re-score. State every deliberate
simplification on the surface an agent reads (a docstring, a                       [20] Karaorman and Abercrombie, "jContractor: Introducing design-by-
schema, a returned string) because an undisclosed one is                                 contract to Java using reflective bytecode instrumentation," Formal
indistinguishable from a bug to the agent conditioned on it and                          Methods in System Design, vol. 27, no. 3, 2005.
to the grader scoring the result. Never let a grader's success
condition be satisfied by a string the harness itself fabricates,                  [21] Bartetzko, Fischer, Mo¨ller, and Wehrheim, "Jass ­ Java with assertions,"
the pattern that let a construct-validity failure survive five prior                     Electronic Notes in Theoretical Computer Science (ENTCS), vol. 55,
audits in this study's most clinically consequential benchmark.                          no. 2, 2001.
A benchmark's published numbers are only as trustworthy as
the layer nobody has been checking.                                                [22] Deng, Ma, Gao, and Sun, "Efficient bug detection by inferring implicit
                                                                                         API contract of pointer state transition," in Proceedings of the IEEE
                             REFERENCES                                                  International Conference on Dependable Systems and Networks (DSN),
                                                                                         2026.
 [1] Long, Du, Xu et al., "LiveClawBench: Benchmarking LLM agents on
      complex, real-world assistant tasks," 2026, preprint.                        [23] Ribeiro, Mamede, and Ferreira, "Systematic API testing through model
                                                                                         checking and executable contracts," in Proceedings of the IEEE Inter-
 [2] Jiang, Black, Geng, Park, Zou, Ng, and Chen, "MedAgentBench: A                      national Conference on Software Testing, Verification and Validation
      virtual EHR environment to benchmark medical LLM agents," NEJM                     (ICST), 2026.
      AI, vol. 2, no. 9, 2025, preprint version (arXiv:2501.14654) read directly;
      Table 3 cited from that version.                                             [24] Alonso, Ernst, Segura, and Ruiz-Corte´s, "Test oracle generation for
                                                                                         REST APIs," ACM Transactions on Software Engineering and Method-
 [3] Tu, Wang, Lu, Huang, Qu, and Mostafavi, "BenchGuard: Who guards                     ology (TOSEM), vol. 35, no. 1, p. art. 19, 2025.
      the benchmarks? automated auditing of LLM agent benchmarks," 2026,
      accepted, Conference on Language Modeling (COLM) 2026.                       [25] Cheon et al., "Enhancing design-by-contract with frame specifications,"
                                                                                         in Proceedings of the 20th International Conference on Software Tech-
 [4] Wang, Bianchi, Zhu, Nie, Kwon, Dhingra, and Zou, "Automated bench-                  nologies (ICSOFT). SCITEPRESS, 2025.
      mark auditing for AI agents and large language models," 2026, preprint.
                                                                                   [26] Ghosal, Jonsson, and Ru¨mmer, "An active learning approach to synthe-
 [5] Bhat, Vaghasiya, Mohsin, and Aali, "Benchmarking the benchmarks: A                  sizing program contracts," in Lecture Notes in Computer Science, vol.
      validity audit of tool-calling evaluation," 2026, preprint.                        14323. Springer, 2023.

 [6] Chen, Yan, Zhang, and Zhang, "Who tests the testers? systematic               [27] Ahmed, Cruz, Imtiaz, Khairunnesa, and Rajan, "Design by contract for
      enumeration and coverage audit of LLM agent tool call safety," 2026,               deep learning APIs," in Proceedings of the 31st ACM Joint European
      preprint.                                                                          Software Engineering Conference and Symposium on the Foundations
                                                                                         of Software Engineering (ESEC/FSE), 2023.
 [7] Wang and Strong, "Beyond accuracy: What data quality means to data
      consumers," Journal of Management Information Systems, vol. 12, no. 4,       [28] Segura, Parejo, Troya, and Ruiz-Corte´s, "Metamorphic testing of REST-
      1996.                                                                              ful web APIs," IEEE Transactions on Software Engineering, vol. 44,
                                                                                         no. 11, 2018.
 [8] Buneman, Khanna, and Tan, "Why and where: A characterization of data
      provenance," in International Conference on Database Theory (ICDT),          [29] Segura, Fraser, Sanchez, and Ruiz-Corte´s, "A survey on metamorphic
      ser. Lecture Notes in Computer Science, vol. 1973. Springer, 2001.                 testing," IEEE Transactions on Software Engineering, vol. 42, no. 9,
                                                                                         2016.
 [9] Liu, Peng, Cao, Wang, Deng, Chen, Yin, and Zhang, "ToolGate:
      Contract-grounded and verified tool execution for LLMs," 2026,               [30] Chen, Kuo, Liu, Tse, and Zhou, "Metamorphic testing: A review of
      preprint.                                                                          challenges and opportunities," ACM Computing Surveys, vol. 51, no. 1,
                                                                                         p. art. 4, 2019.
[10] Bhardwaj, "Agent behavioral contracts: Formal specification and runtime
      enforcement for reliable autonomous AI agents," Zenodo, 2026, preprint,      [31] Atlidakis, Godefroid, and Polishchuk, "RESTler: Stateful REST API
      not peer-reviewed.                                                                 fuzzing," in Proceedings of the 41st International Conference on Soft-
                                                                                         ware Engineering (ICSE), 2019.
[11] Babu and Iyer, "Contract2Tool: Learning preconditions and effects for
      reliable tool-augmented LLM agents," 2026, preprint.                         [32] Godefroid, Lehmann, and Polishchuk, "Differential regression testing for
                                                                                         REST APIs," in Proceedings of the 29th ACM SIGSOFT International
[12] Wang, He, Wang, Xing, De, and Chen, "ContractBench: Can LLM                         Symposium on Software Testing and Analysis (ISSTA), 2020.
      agents preserve observation contracts?" 2026, preprint.
                                                                                   [33] Kapoor, Vaithilingam et al., "AI agents that matter," Transactions on
[13] Milev, Balunovic´, Baader, and Vechev, "ToolFuzz ­ automated agent                  Machine Learning Research (TMLR), 2024.
      tool testing," 2025, preprint.
                                                                                   [34] Reuel, Hardy, Smith, Lamparth, Hardy, and Kochenderfer, "BetterBench:
[14] Iyer and Babu, "The gate is only as honest as its contracts: Contract-              Assessing AI benchmarks, uncovering issues, and establishing best
      Guard for the contract layer of risk-aware causal gating," 2026, preprint.         practices," in NeurIPS Datasets and Benchmarks Track, 2024.

[15] Shah, Morovati, Rahman, and Khomh, "Characterizing faults in agentic          [35] Yu, Zhu, He, and Kang, "UTBoost: Rigorous evaluation of coding agents
      AI: A taxonomy of types, symptoms, and root causes," 2026, preprint.               on SWE-bench," in Proceedings of ACL, 2025.

                                                                                   [36] Gyori, Shi, Hariri, and Marinov, "Reliable testing: Detecting state-
                                                                                         polluting tests to prevent test dependency," in Proceedings of ISSTA,
                                                                                         2015.

                                                                                   [37] Lam, Oei, Shi, Marinov, and Xie, "iDFlakies: A framework for detecting
                                                                                         and partially classifying flaky tests," in Proceedings of ICST, 2019.

                                                                                   [38] Debenedetti, Zhang, Balunovic´, Beurer-Kellner, Fischer, and Trame`r,
                                                                                         "AgentDojo: A dynamic environment to evaluate prompt injection
                                                                                         attacks and defenses for LLM agents," 2024, neurIPS 2024, Scopus-
                                                                                         confirmed; D&B track sub-label unconfirmed.

                                                                                   [39] Ma, Feng, Metz, Lu, Verma, and Dehghan, "MM-ToolSandBox: A
                                                                                         unified framework for evaluating visual tool-calling agents," 2026,
                                                                                         preprint.
